name: 🚀 Deploy Frontend to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🧹 Checkout code
        uses: actions/checkout@v3

      - name: ⚙️ Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: 📦 Install dependencies
        run: |
          echo "🟡 Installing dependencies..."
          cd jarvis-frontend
          npm ci
          echo "✅ Dependencies installed successfully."

      - name: 🏗️ Build project
        run: |
          echo "🟡 Building project.."
          cd jarvis-frontend
          npm run build
          echo "✅ Build complete. Contents of dist folder:"
          ls -la dist

      - name: 🧩 Debug SSH Key presence
        run: |
          echo "🔍 Checking SSH key variable..."
          if [ -z "${{ secrets.FRONTEND_DEPLOY_KEY }}" ]; then
            echo "❌ SSH key secret is missing or not passed!"
            exit 1
          else
            echo "✅ SSH key secret detected."
            echo "🔐 First 100 chars of key:"
            echo "${{ secrets.FRONTEND_DEPLOY_KEY }}" | head -c 100
          fi
          echo "🌍 EC2 Host: ${{ secrets.EC2_HOST }}"
          echo "👤 EC2 User: ${{ secrets.EC2_USER }}"
        shell: bash

      - name: 🌐 Pre-check SSH connection manually
        run: |
          echo "🔍 Testing raw SSH connectivity..."
          echo "${{ secrets.FRONTEND_DEPLOY_KEY }}" > /tmp/ec2_key.pem
          chmod 600 /tmp/ec2_key.pem
          echo "⏳ Trying to connect to ${{ secrets.EC2_HOST }} on port 22..."
          ssh -vvv -o ConnectTimeout=10 -i /tmp/ec2_key.pem ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} "echo ✅ SSH connected successfully." || (echo "❌ SSH connection test failed!" && exit 1)
        shell: bash

      - name: 📤 Deploy to EC2 (SCP)
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.FRONTEND_DEPLOY_KEY }}
          source: "jarvis-frontend/dist/*"
          target: "/usr/share/nginx/html"
          port: 22
          timeout: 60s
          command_timeout: 30s
          debug: true

      - name: 🚀 Verify & restart Nginx
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.FRONTEND_DEPLOY_KEY }}
          script: |
            echo "🟡 Checking Nginx directory permissions..."
            ls -ld /usr/share/nginx/html
            echo "🟡 Listing files..."
            ls -la /usr/share/nginx/html
            echo "🟡 Setting permissions..."
            sudo chown -R ubuntu:www-data /usr/share/nginx/html
            sudo chmod -R 775 /usr/share/nginx/html
            sudo systemctl restart nginx
            echo "✅ Nginx restarted successfully."
